import Phaser from 'phaser';
export class BuildingSprite extends Phaser.GameObjects.Container { constructor(scene:Phaser.Scene,x:number,y:number,label:string,color:number,onClick:()=>void){ super(scene,x,y); const base=scene.add.ellipse(0,18,92,36,0x5faa77,.32); const body=scene.add.rectangle(0,0,68,54,color,1).setStrokeStyle(3,0xffffff,.7); const roof=scene.add.triangle(0,-35,-44,0,44,0,0xf0b66d,1); const text=scene.add.text(0,38,label,{fontFamily:'sans-serif',fontSize:'15px',color:'#315342'}).setOrigin(.5); this.add([base,body,roof,text]); this.setSize(96,92).setInteractive({useHandCursor:true}).on('pointerdown',onClick).on('pointerover',()=>scene.tweens.add({targets:this,scale:1.07,duration:120})).on('pointerout',()=>scene.tweens.add({targets:this,scale:1,duration:120})); scene.add.existing(this);} }
