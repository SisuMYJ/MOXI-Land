import Phaser from 'phaser';
import type { Resident } from '../../types/game';
export class ResidentSprite extends Phaser.GameObjects.Container { constructor(scene:Phaser.Scene,r:Resident,onClick:()=>void){ super(scene,r.position?.x??260,r.position?.y??420); const shadow=scene.add.ellipse(0,19,40,14,0x315342,.18); const body=scene.add.circle(0,0,19,0xffd7a8).setStrokeStyle(3,0xffffff,.85); const face=scene.add.text(0,-1, r.species==='猫'?'🐱':r.species==='狐狸'?'🦊':r.species==='鹿'?'🦌':r.species==='熊'?'🐻':'🐰',{fontSize:'24px'}).setOrigin(.5); const name=scene.add.text(0,29,r.name,{fontSize:'12px',color:'#315342'}).setOrigin(.5); this.add([shadow,body,face,name]); this.setSize(56,62).setInteractive({useHandCursor:true}).on('pointerdown',onClick); scene.add.existing(this); scene.tweens.add({targets:this,y:this.y-10,duration:1300,yoyo:true,repeat:-1,ease:'Sine.inOut',delay:Math.random()*800}); scene.tweens.add({targets:this,x:this.x+Phaser.Math.Between(-20,20),duration:2600,yoyo:true,repeat:-1,ease:'Sine.inOut'}); } }
