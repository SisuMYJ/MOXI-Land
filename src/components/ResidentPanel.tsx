import { chatByPersonality } from '../content/residents';import { useGameStore } from '../store/useGameStore';
export function ResidentPanel(){const {residents,selectedResidentId,chatResident,giftResident}=useGameStore();const r=residents.find(x=>x.id===selectedResidentId); if(!r)return null;return <div><p className="big">{r.species==='猫'?'🐱':r.species==='狐狸'?'🦊':r.species==='鹿'?'🦌':r.species==='熊'?'🐻':'🐰'}</p><h3>{r.name} · 好感度 {r.friendship}</h3><p>{r.currentMood}</p><blockquote>{chatByPersonality[r.personality]}</blockquote><button onClick={()=>chatResident(r.id)}>聊天</button><button onClick={()=>giftResident(r.id)}>送礼（占位）</button></div>}
